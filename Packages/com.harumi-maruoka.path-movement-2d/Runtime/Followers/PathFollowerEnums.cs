namespace Game.PathMovement2D
{
    public enum PathTimingMode { Duration, Speed }
    public enum PathLoopMode { Once, Loop, PingPong }
    public enum PathDirection { Forward, Backward }
    public enum PathUpdateMode { Update, LateUpdate, FixedUpdate }
    public enum PathPlaybackState { Stopped, Playing, Paused }
}
